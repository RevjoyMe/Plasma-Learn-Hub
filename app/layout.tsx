import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Plasma Learn Hub - Educational Gaming Platform",
  description:
    "Interactive educational gaming platform featuring blockchain-powered quizzes and games. Learn about stablecoins, DeFi, and crypto while earning rewards!",
  generator: "v0.app",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
